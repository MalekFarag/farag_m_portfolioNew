const config = {
    host: 'localhost',
    port: 3000, // windows: 3000 // MAC: 8889 // *Change accordingly
    uname: 'root',
    pname: '', // windows: *leave blank* // MAC: root // *Change accordingly
    db: 'db_'
}

module.exports = config;